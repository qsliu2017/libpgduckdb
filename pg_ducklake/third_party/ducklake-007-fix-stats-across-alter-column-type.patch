diff --git a/src/include/storage/ducklake_catalog.hpp b/src/include/storage/ducklake_catalog.hpp
index d5c3734b..b7285a9b 100644
--- a/src/include/storage/ducklake_catalog.hpp
+++ b/src/include/storage/ducklake_catalog.hpp
@@ -265,7 +265,7 @@ public:
 	void CacheInlinedDeletionTableResult(TableIndex table_id, DuckLakeSnapshot snapshot, bool exists);
 
 	//! Invalidate the cached table stats entry for a given stats cache key.
-	void InvalidateTableStatsCache(idx_t next_file_id, TableIndex table_id);
+	void InvalidateTableStatsCache(idx_t next_file_id, idx_t schema_version, TableIndex table_id);
 	//! Invalidate the cached schema entry for a given schema_version.
 	void InvalidateSchemaCache(idx_t schema_version);
 
@@ -278,7 +278,7 @@ private:
 	//! Pin a schema cache entry for the duration of the current query to ensure safe memory access.
 	void PinSchemaForQuery(DuckLakeTransaction &transaction, shared_ptr<DuckLakeSchemaCacheEntry> entry);
 	void LoadNameMaps(DuckLakeTransaction &transaction);
-	string StatsCacheKey(idx_t next_file_id, TableIndex table_id) const;
+	string StatsCacheKey(idx_t next_file_id, idx_t schema_version, TableIndex table_id) const;
 	string SchemaCacheKey(idx_t schema_version) const;
 	string SchemaPinStateKey() const;
 	ObjectCache &GetObjectCacheInstance();
diff --git a/src/storage/ducklake_catalog.cpp b/src/storage/ducklake_catalog.cpp
index 44ef3d4f..8bba4dab 100644
--- a/src/storage/ducklake_catalog.cpp
+++ b/src/storage/ducklake_catalog.cpp
@@ -793,7 +793,12 @@ shared_ptr<DuckLakeTableStats> DuckLakeCatalog::GetTableStats(DuckLakeTransactio
 shared_ptr<DuckLakeTableStats> DuckLakeCatalog::GetTableStats(DuckLakeTransaction &transaction,
                                                               DuckLakeSnapshot snapshot, TableIndex table_id) {
 	auto &cache = GetObjectCacheInstance();
-	auto key = StatsCacheKey(snapshot.next_file_id, table_id);
+	// Key the stats cache on schema_version as well as next_file_id: the cached column stats carry the
+	// column TYPES of the schema they were built against, and a catalog-only ALTER COLUMN ... TYPE bumps
+	// schema_version without advancing next_file_id. Without schema_version in the key, the post-ALTER
+	// read would hit the pre-ALTER entry and serve stats tagged with the old column type, mismatching the
+	// new data files in MergeStats and dropping the table-level min/max (corrupting zone-map reads).
+	auto key = StatsCacheKey(snapshot.next_file_id, snapshot.schema_version, table_id);
 	auto cached = cache.Get<DuckLakeTableStatsCacheEntry>(key);
 	if (cached) {
 		auto *raw = cached.get();
@@ -1071,17 +1076,17 @@ void DuckLakeCatalog::CacheInlinedDeletionTableResult(TableIndex table_id, DuckL
 	}
 }
 
-string DuckLakeCatalog::StatsCacheKey(idx_t next_file_id, TableIndex table_id) const {
-	return StringUtil::Format("ducklake:%s:%s:%s:stats:%llu:table:%llu", GetName(), MetadataPath(), instance_id,
-	                          next_file_id, table_id.index);
+string DuckLakeCatalog::StatsCacheKey(idx_t next_file_id, idx_t schema_version, TableIndex table_id) const {
+	return StringUtil::Format("ducklake:%s:%s:%s:stats:%llu:sv:%llu:table:%llu", GetName(), MetadataPath(), instance_id,
+	                          next_file_id, schema_version, table_id.index);
 }
 
 string DuckLakeCatalog::SchemaCacheKey(idx_t schema_version) const {
 	return StringUtil::Format("ducklake:%s:%s:%s:schema:%llu", GetName(), MetadataPath(), instance_id, schema_version);
 }
 
-void DuckLakeCatalog::InvalidateTableStatsCache(idx_t next_file_id, TableIndex table_id) {
-	GetObjectCacheInstance().Delete(StatsCacheKey(next_file_id, table_id));
+void DuckLakeCatalog::InvalidateTableStatsCache(idx_t next_file_id, idx_t schema_version, TableIndex table_id) {
+	GetObjectCacheInstance().Delete(StatsCacheKey(next_file_id, schema_version, table_id));
 }
 
 void DuckLakeCatalog::InvalidateSchemaCache(idx_t schema_version) {
diff --git a/src/storage/ducklake_metadata_manager.cpp b/src/storage/ducklake_metadata_manager.cpp
index 685b9c8b..8e8c1f79 100644
--- a/src/storage/ducklake_metadata_manager.cpp
+++ b/src/storage/ducklake_metadata_manager.cpp
@@ -5109,7 +5109,7 @@ WHERE NOT EXISTS (
 	auto &catalog = transaction.GetCatalog();
 	for (auto &snapshot : snapshots) {
 		for (auto &table_id : stats_table_ids) {
-			catalog.InvalidateTableStatsCache(snapshot.next_file_id, table_id);
+			catalog.InvalidateTableStatsCache(snapshot.next_file_id, snapshot.schema_version, table_id);
 		}
 	}
 }
diff --git a/src/storage/ducklake_stats.cpp b/src/storage/ducklake_stats.cpp
index 1d94d7ed..ec9ba88d 100644
--- a/src/storage/ducklake_stats.cpp
+++ b/src/storage/ducklake_stats.cpp
@@ -99,6 +99,21 @@ DuckLakeColumnStats DuckLakeColumnStats::FromGlobalStats(const LogicalType &type
 	return stats;
 }
 
+// Reconcile a string-encoded stat value to a (possibly new) target type. Returns true if the value
+// remains valid under the target type. After ALTER COLUMN ... TYPE the table-level seed stats carry the
+// pre-ALTER type while the new data file carries the post-ALTER type; casting (e.g. INTEGER 1 -> BIGINT 1)
+// keeps the value comparable instead of discarding it, which would drop the table-level min/max and
+// drive a zone-map mis-decode on read.
+static bool ReconcileStatToType(string &value, const LogicalType &target) {
+	Value casted;
+	string error;
+	if (Value(value).DefaultTryCastAs(target, casted, &error) && !casted.IsNull()) {
+		value = casted.ToString();
+		return true;
+	}
+	return false;
+}
+
 void DuckLakeColumnStats::MergeStats(const DuckLakeColumnStats &new_stats) {
 	bool types_differ = type != new_stats.type;
 	if (types_differ) {
@@ -126,12 +141,20 @@ void DuckLakeColumnStats::MergeStats(const DuckLakeColumnStats &new_stats) {
 		}
 	}
 
-	if (!new_stats.AnyValid()) {
-		// all values in the source are NULL - don't update min/max
-		if (types_differ) {
+	if (types_differ) {
+		// The column was retyped (e.g. ALTER COLUMN ... TYPE). Cast the existing min/max to the new
+		// type so they stay comparable below; only invalidate the ones that cannot be represented in
+		// the new type. Discarding them outright (the previous behavior) dropped the table-level min/max
+		// and corrupted zone-map driven reads of pre-ALTER rows.
+		if (has_min && !ReconcileStatToType(min, type)) {
 			has_min = false;
+		}
+		if (has_max && !ReconcileStatToType(max, type)) {
 			has_max = false;
 		}
+	}
+	if (!new_stats.AnyValid()) {
+		// all values in the source are NULL - don't update min/max
 		return;
 	}
 	if (!AnyValid()) {
@@ -143,43 +166,37 @@ void DuckLakeColumnStats::MergeStats(const DuckLakeColumnStats &new_stats) {
 		any_valid = true;
 		return;
 	}
-	if (types_differ) {
-		// min/max from different types cannot be compared - invalidate them
+	if (!new_stats.has_min) {
 		has_min = false;
-		has_max = false;
-	} else {
-		if (!new_stats.has_min) {
-			has_min = false;
-		} else if (has_min) {
-			// both stats have a min - select the smallest
-			if (RequiresValueComparison(type)) {
-				// for numerics/temporals we need to parse the stats
-				auto current_min = Value(min).DefaultCastAs(type);
-				auto new_min = Value(new_stats.min).DefaultCastAs(type);
-				if (new_min < current_min) {
-					min = new_stats.min;
-				}
-			} else if (new_stats.min < min) {
-				// for other types we can compare the strings directly
+	} else if (has_min) {
+		// both stats have a min - select the smallest
+		if (RequiresValueComparison(type)) {
+			// for numerics/temporals we need to parse the stats
+			auto current_min = Value(min).DefaultCastAs(type);
+			auto new_min = Value(new_stats.min).DefaultCastAs(type);
+			if (new_min < current_min) {
 				min = new_stats.min;
 			}
+		} else if (new_stats.min < min) {
+			// for other types we can compare the strings directly
+			min = new_stats.min;
 		}
+	}
 
-		if (!new_stats.has_max) {
-			has_max = false;
-		} else if (has_max) {
-			// both stats have a max - select the largest
-			if (RequiresValueComparison(type)) {
-				// for numerics/temporals we need to parse the stats
-				auto current_max = Value(max).DefaultCastAs(type);
-				auto new_max = Value(new_stats.max).DefaultCastAs(type);
-				if (new_max > current_max) {
-					max = new_stats.max;
-				}
-			} else if (new_stats.max > max) {
-				// for other types we can compare the strings directly
+	if (!new_stats.has_max) {
+		has_max = false;
+	} else if (has_max) {
+		// both stats have a max - select the largest
+		if (RequiresValueComparison(type)) {
+			// for numerics/temporals we need to parse the stats
+			auto current_max = Value(max).DefaultCastAs(type);
+			auto new_max = Value(new_stats.max).DefaultCastAs(type);
+			if (new_max > current_max) {
 				max = new_stats.max;
 			}
+		} else if (new_stats.max > max) {
+			// for other types we can compare the strings directly
+			max = new_stats.max;
 		}
 	}
 
